const config = {
  routes: {
    home: "/",
    products: "/products",
    productDetail: "/products/:slug",
    notfound: "*",
    register: "/register",
    login: "/login",
  },
};

export default config;
